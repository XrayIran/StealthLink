[package]
name = "stealthlink-crypto"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "stealthlink_crypto"
crate-type = ["cdylib", "staticlib"]

[dependencies]
chacha20poly1305 = { version = "0.10", features = ["std"] }
rand = "0.8"
libc = "0.2"

[dev-dependencies]
hex = "0.4"
